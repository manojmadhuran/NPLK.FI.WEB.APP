@model List<FI.PORTAL.dbconnect.NEW_CUS_HEADER>

@{
    ViewBag.Title = "Index";
    String currentSort = ViewBag.sortString;
}

<div class="row">
    <div class="col-md-12">
        <div class="card">

            <div class="card-header">
                <form action="@Url.Action("", "CustomerRegistration")" method="get">
                    <div class="form-row align-items-center">
                        <div class="col-auto my-1">
                            <select class="custom-select mr-sm-2" name="sortby">
                                @if (currentSort == "All")
                                {
                                    <option value="All" selected>All</option>
                                    <option value="Pending">Pending</option>
                                    <option value="Forwarded">Forwarded/Reversed</option>
                                }
                                else if (currentSort == "Pending")
                                {
                                    <option value="All">All</option>
                                    <option value="Pending" selected>Pending</option>
                                    <option value="Forwarded">Forwarded/Reversed</option>
                                }
                                else if (currentSort == "Forward/Reverse")
                                {
                                    <option value="All">All</option>
                                    <option value="Pending">Pending</option>
                                    <option value="Forwarded" selected>Forwarded/Reversed</option>
                                }
                                else
                                {
                                    <option value="All" selected>All</option>
                                    <option value="Pending">Pending</option>
                                    <option value="Forwarded">Forwarded/Reversed</option>
                                }

                            </select>
                        </div>
                        <div class="col-auto my-1">
                            <button type="submit" class="btn btn-primary">Sort </button>
                        </div>
                    </div>
                </form>
            </div>

        </div>
    </div>
</div>



<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h4>New Customer Registration Requests</h4>
            </div>
            <div class="card-body">

                <div id="secposts" class="row">
                    <div class="col-md-12">
                        <table class="table table-bordered small" style="width:100%">
                            <thead>
                                <tr>
                                    <th>Request ID</th>
                                    <th>Current Status</th>
                                    <th>Customer Name</th>
                                    <th>Sales Area Code</th>
                                    <th>Sales Area Name</th>
                                    <th>Created Date</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Count == 0)
                                {
                                    <tr>
                                        <td colspan="7">No Records Found!</td>
                                    </tr>

                                }
                                else
                                {
                                    foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.CusReqID</td>
                                            <td>@item.CurrentStat</td>
                                            <td>@item.CusName</td>
                                            <td>@item.SalesCode</td>
                                            <td>@item.SalesAreaName</td>
                                            <td>@item.CreatedDate</td>
                                            <td>
                                                <a href="@Url.Action("CustomerRequestFormView", new { item.CusReqID})" class="btn btn-success btn-sm">View</a>
                                            </td>
                                        </tr>
                                    }
                                }

                            </tbody>
                        </table>
                    </div>
                </div>


            </div>
        </div>
    </div>
</div>
